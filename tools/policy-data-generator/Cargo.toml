[package]
name = "policy-data-generator"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = { workspace = true }
clap = { version = "4.5.40", features = ["derive"] }
policy-styx-lib = { workspace = true, features = ["runtime"] }
