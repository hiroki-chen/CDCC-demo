[package]
name = "policy-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
uuid = { workspace = true }
policy-styx-lib = { workspace = true, default-features = false }
bincode = { version = "1.3.3", default-features = false }
